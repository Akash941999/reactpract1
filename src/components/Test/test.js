import React from 'react'

export default function profile(prop){

    return(
        <div>
            <h1>Hii I am from outside</h1>
            <h1>{prop.text.name}</h1>
            <h1>{prop.text.name}</h1>

        </div>
    )
}